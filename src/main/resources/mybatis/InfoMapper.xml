<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.InfoMapper">

    <insert id="addInfo" useGeneratedKeys="true" keyProperty="id">
        insert into infos(game_name,
                          equipment_number,
                          location_of_equipment
                         )
                    VALUES(#{game_name},
                           #{equipment_number},
                           #{location_of_equipment}
                          )
    </insert>
    <select id="getInfo" parameterType="java.util.Map" resultType="com.example.entity.info.InfoBean">
        select * from infos
    </select>

    <delete id="deleteInfo" parameterType="int">
        delete from infos where id=#{id}
    </delete>
</mapper>